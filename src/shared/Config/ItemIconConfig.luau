--!strict

-- Paste numeric Roblox image asset IDs here. Empty IDs use the configured glyph.
return table.freeze({
	MagnetBasic = table.freeze({ AssetId = "", Glyph = "U", Color = Color3.fromRGB(239, 74, 74) }),
	AdvancedMagnet = table.freeze({
		AssetId = "",
		Glyph = "A",
		Color = Color3.fromRGB(42, 199, 214),
	}),
	QuantumMagnet = table.freeze({
		AssetId = "",
		Glyph = "Q",
		Color = Color3.fromRGB(255, 76, 167),
	}),
	CashPotion = table.freeze({ AssetId = "", Glyph = "$", Color = Color3.fromRGB(255, 180, 42) }),
	StrengthPotion = table.freeze({
		AssetId = "",
		Glyph = "+",
		Color = Color3.fromRGB(139, 84, 246),
	}),
	Money = table.freeze({ AssetId = "", Glyph = "$", Color = Color3.fromRGB(255, 154, 47) }),
	Gears = table.freeze({ AssetId = "", Glyph = "⚙", Color = Color3.fromRGB(139, 84, 246) }),
})
